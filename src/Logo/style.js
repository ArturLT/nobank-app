import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    boxLogo:{
        marginBottom: 10,
        paddingTop: 30,
        paddingBottom: 10,
        alignItems: 'center',
        
    },
    logo:{
        width: 120,
        height: 50,
    }
})

export default styles;